# Android Bitcoin Wallet - converted from JavaFX wallettemplate

1. Đây là bản chuyển từ MainController.java (JavaFX) sang Android
2. Chạy `./gradlew assembleDebug` hoặc push lên GitHub để Action tự build APK
3. Wallet đang dùng TestNet3 - đổi sang MainNetParams nếu muốn mainnet
